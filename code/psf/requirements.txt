# psfselect — PSF estimation & model selection
numpy>=1.21
scipy>=1.7
pandas>=1.3
tifffile>=2023.1
matplotlib>=3.4
pyyaml>=5.4
tabulate>=0.9            # used by pandas.to_markdown in the report
psfmodels>=0.3.0         # pure-Python theoretical PSF backend (Born&Wolf/GL/RW analogues)
scikit-image>=0.19       # Richardson-Lucy deconvolution + marching_cubes (notebook)

# Optional, for tests
pytest>=6.2

# NOTE: the EPFL PSF Generator is a Java application, not a pip package.
# Download PSFGenerator.jar from https://bigwww.epfl.ch/algorithms/psfgenerator/
# and point PSF_GENERATOR_JAR at it (requires a Java runtime, e.g. Java 8+).
